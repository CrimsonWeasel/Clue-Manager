using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ClueManager.Data
{
    public class SaveClueResponse
    {   //Wait and finish this later when other tasks are finished. Putting Save back the way it was for now. 
        //public bool SaveSuccessful { get; set; }
        ClueDetails _clueDetails;
        private SaveClueRequest _request;

        public SaveClueResponse(SaveClueRequest request) 
        { }


    }
}
